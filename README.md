# NeuralIR
Python-based framework for the generation of a special form of IR for C / CPL / C++ / Python / etc languages.

# GUI
To run the GUI version, use:
```bash
./run_gui.sh
```

or

```bash
python3 -m gui.inliner
```